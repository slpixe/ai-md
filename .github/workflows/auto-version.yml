name: Auto Version
on:
  workflow_dispatch:
    inputs:
      version-type:
        description: 'Version type (major/minor/patch)'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major

jobs:
  test:
    name: test  # This name matches the required status check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
      - name: Install dependencies
        run: npm ci
      - name: Install global dependencies
        run: npm install -g tsx
      - name: Run tests
        run: npm test

  version:
    needs: test  # Wait for test job to complete
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    permissions:
      contents: write
      statuses: write  # Allow setting commit statuses
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.ref }}  # Explicitly checkout the target branch

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'

      - name: Install dependencies
        run: npm ci

      - name: Configure Git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

      # Update SHA to latest before pushing
      - name: Update to latest
        run: git pull --rebase

      - name: Bump version
        run: |
          # Create the version bump commit
          npm version ${{ github.event.inputs.version-type }} --no-git-tag-version --ignore-scripts
          git add package.json package-lock.json
          git commit -m "chore(release): v$(node -p "require('./package.json').version")"
          
          # Create tag for the new version
          git tag v$(node -p "require('./package.json').version")
          
          # Push changes
          git push
          git push --tags
